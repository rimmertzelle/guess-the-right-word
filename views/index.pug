extends layout

block header    
    nav(class=['breadcrumb'], aria-label='breadcrumbs')
        ul
            li(class='is-active')
                a(href='./' aria-current='page') Home

block content
    div.container
        div(class=['columns', 'is-centered'])
            div(class=['column', 'has-text-centered', 'is-half'])
                h1(class=['title', 'is-1']) Learning English Words
                p Hi, with this site you can practise your English words
        div(class=['columns', 'is-centered'])
            div(class=['column', 'has-text-centered', 'is-half'])
                a(class=['button', 'is-link'] href='./learning') start